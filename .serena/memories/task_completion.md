# Task Completion

- For frontend changes, run `npm run build`.
- Run `npm run test` when smoke-test expectations are relevant or updated.
- For visual/3D changes, start `npm run dev` and inspect at least one desktop and one mobile viewport in the browser; confirm canvas/3D area is nonblank and not covering critical UI.
- Note any verification command that cannot be run and why.