# Core

- Root `D:\Ai Project\Demo` is a mixed workspace; active runtime app is the top-level Vite frontend in `src/` with `index.html`, `vite.config.js`, and `package.json`.
- Other sizable folders such as `notebooklm-py/`, `outputs/`, and `portfolio-beauty-consultant/` are separate artifacts/modules; avoid reading them for top-level frontend tasks unless explicitly targeted.
- Project-wide rules live in `AGENTS.md`; for cinematic frontend work also read repo skill `mem:frontend/premium_3d`.
- Top-level repo is currently not a git repository; git commit/log/status commands fail from the root.